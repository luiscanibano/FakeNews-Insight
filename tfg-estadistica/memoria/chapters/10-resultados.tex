\chapter{Resultados y discusión}\label{cap:resultados}

\section{Comparativa global de modelos}

\begin{table}[h]
  \centering
  \begin{tabular}{lcccc}
    \toprule
    Modelo & Acc. & F1 macro & NLL & ECE \\
    \midrule
    TF-IDF + LR        & 0.5081 & 0.4842 & 1.0897 & 0.1585 \\
    DeBERTa-v3-small   & 0.7281 & 0.7231 & 0.8070 & 0.1277 \\
    \bottomrule
  \end{tabular}
  \caption{Métricas finales sobre la partición de prueba de FEVER-NLI.}
  \label{tab:resultados-globales}
\end{table}

DeBERTa-v3-small incrementa el F1 macro en 0.2389 puntos absolutos
respecto al baseline lineal. La mejora es coherente en exactitud, NLL,
ECE y Brier (tabla~\ref{tab:calibration}), lo que indica que no se
trata sólo de un cambio en el umbral de decisión, sino de una mejor
modelización condicional del par claim--evidencia.

\section{Análisis por clase}

\begin{table}[h]
  \centering
  \begin{tabular}{lccc}
    \toprule
    Modelo & F1 \textsc{sup} & F1 \textsc{ref} & F1 \textsc{nei} \\
    \midrule
    TF-IDF + LR       & 0.5690 & 0.5287 & 0.3550 \\
    DeBERTa-v3-small  & 0.8019 & 0.7417 & 0.6257 \\
    \bottomrule
  \end{tabular}
  \caption{Desglose por clase del F1.}
  \label{tab:resultados-por-clase}
\end{table}

La clase \textsc{nei} continúa siendo la más difícil, incluso con el
modelo neuronal. En la matriz de confusión de DeBERTa-v3-small
(figura~\ref{fig:deberta-confusion}), 752 ejemplos \textsc{nei} se
predicen como \textsc{sup} y 639 como \textsc{ref}. Esto confirma que
la ausencia de información suficiente se confunde con solapamiento
semántico parcial o contradicción local.

\begin{figure}[h]
  \centering
  \includegraphics[width=0.75\textwidth]{../../research-stats/reports/figures/deberta_v1_confusion.png}
  \caption{Matriz de confusión de DeBERTa-v3-small sobre prueba.}
  \label{fig:deberta-confusion}
\end{figure}

\section{Calibración}

El diagnóstico de calibración se presenta en el
capítulo~\ref{cap:calibracion}. DeBERTa-v3-small obtiene ECE 0.1277,
menor que el baseline (0.1585), aunque aún muestra sobreconfianza en
algunos intervalos de probabilidad alta. Por ello las probabilidades se
utilizan como señales de ranking y confianza relativa, no como
probabilidades perfectamente calibradas.

\section{Contrastes estadísticos}

Conforme al capítulo~\ref{cap:tests}:
\begin{itemize}[leftmargin=*,itemsep=0.2em]
  \item Test de McNemar DeBERTa vs.\ baseline:
    $\chi^2 = 1532.2167$, $p < 10^{-300}$.
  \item Bootstrap $\Delta F1_{\text{macro}}$:
    IC 95\,\% $= [\,0.2289,\;0.2503\,]$.
\end{itemize}

\section{Curva precisión--cobertura por umbral $\tau_{\text{conf}}$}

Se reporta, para distintos valores de $\tau_{\text{conf}} \in
\{0.5, 0.55, 0.6, 0.7, 0.8\}$, la fracción de \emph{claims} con
veredicto $\neq$ \textsc{nei} (cobertura) y la precisión sobre los
veredictos emitidos. La elección final de $\tau_{\text{conf}}$
corresponde al punto de operación que maximiza F1 sobre claims con
veredicto.

\section{Análisis de errores}

El análisis agregado de errores muestra tres patrones principales:

\begin{itemize}[leftmargin=*,itemsep=0.2em]
  \item \textbf{Solapamiento léxico insuficiente.} El baseline tiende a
    predecir \textsc{sup} cuando claim y evidencia comparten muchas
    palabras, incluso si la relación lógica es neutra. DeBERTa
    corrige buena parte de estos casos gracias al modelado
    contextual.
  \item \textbf{Clase \textsc{nei}.} El error dominante de DeBERTa se
    concentra en distinguir evidencia incompleta de evidencia
    favorable o contradictoria. Es una limitación esperable porque
    \textsc{nei} requiere razonar sobre lo que la evidencia no dice.
  \item \textbf{Evidencias parciales.} Cuando una evidencia contiene una
    entidad correcta pero omite el atributo decisivo (fechas,
    nacionalidad, profesión o relaciones), el modelo puede asignar
    demasiada confianza a \textsc{sup}. Este patrón explica parte
    del ECE observado.
\end{itemize}

En sentido inverso, los 442 casos donde el baseline acierta y DeBERTa
falla sugieren que algunas regularidades léxicas simples siguen siendo
útiles, especialmente en formulaciones muy cortas o altamente
estereotipadas.

\section{Limitaciones}

\begin{itemize}[leftmargin=*,itemsep=0.2em]
  \item FEVER se construye sobre Wikipedia y por tanto el dominio es
        enciclopédico; la generalización a dominio periodístico no se
        evalúa aquí.
  \item El supuesto de independencia condicional de evidencias
        (capítulo~\ref{cap:agregacion}) es una aproximación.
  \item No se evalúa la subtarea de recuperación; se asumen evidencias
        ya disponibles.
\end{itemize}
